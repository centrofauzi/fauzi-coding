{
  "name": "express-js",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.1",
    "nodemon": "^2.0.20"
  },

  "scripts": {
    "dev": "nodemon index.js" //restart otomatis
  }
}


- control + C : force close server


const express = require('express');
const app = express();
const router = require('./router');

app.use(express.json())
app.use(express.urlencoded({ extended: true}))  

app.use(router);
app.listen(8000, () => {
    console.log(`server is running at ${8000}`)
});